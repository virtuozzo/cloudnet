defaults: &defaults
  uri: <%= ENV["ONAPP_CP"] %>
  user: <%= ENV["ONAPP_USER"] %>
  pass: <%= ENV["ONAPP_PASS"] %>
  user_role: <%= ENV["ONAPP_ROLE"] %>

development:
  <<: *defaults

test:
  <<: *defaults

staging:
  <<: *defaults

production:
  <<: *defaults
