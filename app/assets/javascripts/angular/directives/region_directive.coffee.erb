@app.directive "regionFilter", ($timeout) ->
  restrict: 'E'
  templateUrl: "<%= asset_path('templates/server_search/regions.html') %>"
  scope:
    state: '='
    
  link: ($scope, $el, $attr) ->
    $timeout ->
      $('input[type=radio]').click()
    ,0