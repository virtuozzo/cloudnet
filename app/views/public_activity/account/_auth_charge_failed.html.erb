<% card = BillingCard.with_deleted.find(activity.parameters[:card]) %>
Authorize charge failed on Card ID: <%= card.id %> (Last 4: <%= card.last4 %>) for <%= Invoice.pretty_total(activity.parameters[:amount] * Invoice::CENTS_IN_DOLLAR) rescue nil %>. Reason: <%= activity.parameters[:reason][0..100] %>