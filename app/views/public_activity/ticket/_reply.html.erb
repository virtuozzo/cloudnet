<%= link_to activity.owner.full_name, admin_user_path(activity.owner) %> (IP: <%= activity.parameters[:ip] %>) replied to ticket (ID: <%= activity.parameters[:reference] %>)

<% if activity.trackable %>
  <%= link_to activity.trackable.subject, activity.trackable %>
<% else %>
  which has since been removed
<% end %>