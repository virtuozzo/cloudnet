<%= link_to activity.owner.full_name, admin_user_path(activity.owner) %> (IP: <%= activity.parameters[:ip] %>) created server (ID: <%= activity.trackable_id %>)

<% if activity.parameters[:provisioned]%>
  as 'one click app', role: <%= activity.parameters[:provisioned]%>,
<% end %>

<% if activity.parameters[:api]%>
  by API call,
<% end %>

<% if activity.trackable %>
  <%= link_to activity.trackable.name, activity.trackable %>
<% else %>
  which has since been removed
<% end %>