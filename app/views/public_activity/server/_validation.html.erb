<%= link_to activity.owner.full_name, admin_user_path(activity.owner) %>'s server (ID: <%= activity.trackable_id %>) was placed under validation for <i><%= Account::FraudValidator::VALIDATION_REASONS[activity.parameters[:reason].to_i] %></i>