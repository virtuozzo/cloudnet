<%= link_to activity.owner.full_name, admin_user_path(activity.owner) %> (IP: <%= activity.parameters[:ip] %>) created <%= activity.parameters[:type] %> record for domain <%= activity.parameters[:domain] %>